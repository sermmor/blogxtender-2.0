/**
 * PreviewSection — retired in BlogXtender 2.0.
 * Preview is now handled inline in EditorPanel (Edit/Preview toggle in the header).
 * This file is kept to avoid breaking any external references.
 */
const PreviewSection = () => null;
export default PreviewSection;
